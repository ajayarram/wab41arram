extends layout

block content
  h1= title
  p Welcome  #{title}
  button(type = "button" class = "btn btn-danger" onclick="update()") Press Me
  div(id="target" class="container text-center m-2 p-3" style="color:red;font-weight:600; background-color:yellow;") display  
  script.
      var client_count=0 
      function update(){
        client_count++;
        document.getElementById("target").innerHTML=`Value now ${client_count}`
      } 
  a(href="cow" ) About Cows